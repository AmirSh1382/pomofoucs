import React from 'react';

const Timer = () => {

    const startTimer = () => {
        
    }

    const stopTimer = () => {

    }

    return (
        <div>
            <button onClick={startTimer}>
                start
            </button>
            <button onClick={stopTimer}>
                stop
            </button>
        </div>
    );
};

export default Timer;